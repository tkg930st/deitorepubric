# アルゴリズム・トレードシステム 要件定義書 (Ver 14.1)

## 1. システム概要
本システムは、日本株市場を対象とした「マルチ銘柄対応・自律型デイトレード・シグナルエンジン」である。バックテストによるパラメータ最適化（analyzer.py）と、リアルタイム・モニタリング（monitor.py）の2フェーズ構成により、期待利益の最大化を目指す。将来的な複数ユーザーへのシグナル配信プラットフォーム化を視野に入れた拡張性を備える。

## 2. 業務フローと運用制約
### 2.1 実行スケジュール
* **稼働日**: 市場営業日に準ずる。祝日はスキップする。
* **特殊運用**: 半ドン（大納会等）は例外的に実行を継続する。
* **セッション管理**: AM（前場）およびPM（後場）の2セッション制。GitHub Actionsによる自動実行を行い、セッション間のデータ整合性はリポジトリへの自動コミットによって担保する。

### 2.2 異常時の挙動
* **データ取得失敗**: ログに記録を残した上で、当該銘柄の取引は見送る。
* **異常値検知**: 取得データに異常が認められる場合は取引を行わない。
* **エラー通知**: 実行自体の停止（ワークフロー失敗）を除き、処理継続中に発生したエラーはDiscordを通じて通知する。事実の通知のみを行い、手動決済への誘導は含まない。

## 3. 機能要件
### 3.1 戦略構築（Analyzerフェーズ）
* **銘柄選定**: 売買代金（50億円以上）および株価（500円以上）によるスクリーニングを実施。
* **優先順位**: 流動性と期待利益が競合する場合、期待利益が高い銘柄を優先する。
* **最適化**: ハイブリッド・バックテストおよび強力再試行ループを用いて、期待利益5%以上のパラメータを探索する。
* **パフォーマンス目標**: 解析フェーズ全体の処理時間は3時間を上限とする。

### 3.2 監視および執行（Monitorフェーズ）
* **エントリーフィルター**: 
    * RSI過熱/売られ過ぎによる制限。
    * VWAP乖離率制限（2.5%超は禁止）。
    * 出来高急増フィルター（直近10本平均の2.0倍以上）。
* **セクターアライメント**: 
    * ライバル5銘柄との単純な騰落相関によるスコア加点（+15点）を行う。
    * 将来的に指数と個別の「親子関係」に基づくフィルタリングへの拡張を検討。
* **エグジットロジック**:
    * TP1（ATR 1.5倍）で50%利確。
    * 残り50%はシャンデリア出口（直近5本の高値/安値 - ATR 2.5倍）によるトレーリングで利益を追及。

## 4. リスク管理要件
### 4.1 資金保護（ハードリミット）
* **デイリー・ストップロス**: 1日の通算損益が **-3.0%** に達した場合、当日の運用（新規エントリー）を緊急停止する。
* **RR比保証**: シグナル送信時に「利確幅(tp_mul) ≥ 損切幅(sl_mul) × 1.5」を強制調整する。
* **VIXリスク調整**: VIX指数20超過時、エントリー閾値を厳格化し、推奨ロットを通常の50%に削減する。

## 5. 今後の拡張ロードマップ
1. **セクター親子関係の定義**: 指数と個別株の主従関係に基づくフィルタリングの実装。
2. **マルチデータソース化**: yfinance以外のデータソース（公式API等）へのフォールバック。
3. **プラットフォーム拡張**: 複数ユーザーへの個別通知インフラの整備。
