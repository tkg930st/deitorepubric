# システム設計書：deitorepubric (Ver 14.1)

## 1. システムアーキテクチャ
本システムは、GitHub Actionsを基盤としたサーバーレスな自動トレードシグナル生成システムである。データ取得、解析、監視、通知の各コンポーネントが疎結合に連携し、リポジトリへの自動コミットによって状態を永続化する。

### 1.1 コンポーネント構成
* **Analyzer (戦略構築)**: `analyzer.py`, `backtest_engine.py`
    * 市場データのスクリーニングと、銘柄ごとの最適パラメータの算出を行う。
* **Monitor (実機監視)**: `monitor.py`, `position_manager.py`
    * リアルタイムデータに基づくエントリー・エグジット判定を行う。
* **Core Logic (共通基盤)**: `utils.py`, `config.py`
    * テクニカル指標計算、Discord通知、共通定数管理を担う。
* **Reviewer (自動改善・監査)**: `gemini_reviewer.py`
    * 実行データと設計書を突き合わせ、運用監査とパラメータの改善提案を行う。

## 2. データ設計
### 2.1 戦略設定 (`best_config.json`)
* `details` 配列内に各銘柄の最適化パラメータ、セクター情報、および「ライバル銘柄リスト」を格納する。
* **拡張設計**: 親子関係の定義を見据え、将来的に `parent_ticker` フィールドを追加可能な構造とする。

### 2.2 ポジション管理 (`positions.json`)
* `ticker`, `side`, `entry_price`, `tp1_hit`, `trailing_stop` 等、現在保有中の仮想ポジションの状態を保持する。

### 2.3 取引結果記録 (`trade_results.csv`)
* `exit_time`, `total_profit`, `exit_reason` 等を記録し、日次サマリー通知のソースとして利用する。

## 3. アルゴリズム詳細設計
### 3.1 エントリー判定
1. **重み付けスコアリング**: RSI, VWAP, RVOL, ADXの各指標に重みを掛け合わせ、`threshold` 超過でシグナル候補とする。
2. **動的調整フィルター**:
    * **VIX調整**: VIX > 20時に閾値を厳格化し、ロットを50%に制限する。
    * **ATR調整**: 現在のATRに基づき、SL/TP幅を動的に伸縮させる。
    * **セクターアライメント**: ライバル銘柄（5銘柄）の騰落一致状況に基づき、スコアを加点する。

### 3.2 エグジットおよびトレーリング
* **分割決済 (TP1)**: ATR 1.5倍到達時に50%を利確し、SLを建値に移動（Breakeven）する。
* **シャンデリア出口 (TP2)**: 残り50%に対し、直近5本の最高値/最安値からATR 2.5倍の位置でトレーリングを行う。

## 4. リスク管理設計（ハードリミット）
### 4.1 デイリー・ストップロス
* **判定ロジック**: 「当日の確定損益」と「現在保有ポジションの含み損益」を合算したトータル損益をリアルタイムで監視する。
* **緊急停止**: 合計損益が **-3.0%** に達した時点で、全ポジションをクローズし、当日の新規エントリーを一切禁止する。

## 5. 運用設計
* **セッション管理**: 前場（AM）および後場（PM）のワークフローを分離実行。各セッション終了時に `positions.json` を含む状態ファイルをリポジトリへ自動コミットし、データの継続性を担保する。
* **配信設計**: 全ユーザーに対し、システムが選定した同一の監視銘柄（最大8銘柄）のシグナルを一斉配信する。
* **エラーハンドリング**: 処理中のエラーはDiscordへ事実のみ通知し、ユーザーによる迅速な状況把握を支援する。

## 6. AIレビュー・監査設計
### 6.1 データ入力定義
ソースコード（.pyファイル）の読み込みは行わず、以下のファイル群のみをGemini APIへ送信する。
* **ドキュメント**: `REQUIREMENTS.md`, `SYSTEM_DESIGN.md`（監査基準）
* **ログデータ**: `execution_log.txt`（当日実行分のみ抽出して送信）
* **パフォーマンスデータ**: `trade_results.csv`, `trade_journal.csv`（直近の傾向分析用）
* **環境設定**: `best_config.json`（現在の最適化パラメータ）

### 6.2 期待される出力（プロンプト構造）
1. **運用監査**: 実行ログとドキュメントを照合し、設計要件（デイリーストップロスの発動条件、各フィルターの挙動など）との乖離やエラーが発生していないかを報告させる。
2. **傾向分析**: トレード時間帯や指標数値ごとの勝敗傾向を統計的に言語化させる。
3. **チューニング提案**: 分析結果に基づき、期待値を向上させるための具体的な設定値（RSI閾値やTP倍率など）の変更案を提示させる。
