# システム設計書：deitorepubric (Ver 15.8)

## 1. システムアーキテクチャ
本システムは、GitHub Actionsを基盤としたサーバーレスな自動トレードシグナル生成システムである。データ取得、解析、監視、通知の各コンポーネントが疎結合に連携し、状態を永続化する。

### 1.1 コンポーネント構成
* **Analyzer (戦略構築)**: `analyzer.py`, `backtest_engine.py`
    * 市場データのスクリーニングと、銘柄ごとの最適パラメータの算出を行う。
* **Test Tool (高速検証)**: `test_analyzer.py`
    * 全銘柄スキャンをスキップし、厳選50銘柄に対して直接解析プロセスを検証する。
    * **同期ルール**: `analyzer.py` の解析ロジックが変更された際、本ファイルも自動的に同期更新される。
* **Monitor (実機監視)**: `monitor.py`, `position_manager.py`
    * リアルタイムデータに基づくエントリー・エグジット判定を行う。
* **Core Logic (共通基盤)**: `utils.py`, `config.py`
    * テクニカル指標計算、Discord通知、共通定数管理を担う。

## 2. データ設計
### 2.1 戦略設定 (`best_config.json`, `test_best_config.json`)
* 各銘柄の最適化パラメータ、個別フィルターフラグ（`use_rsi_filter`, `use_vwap_filter`）を保持する。

## 3. アルゴリズム詳細設計
### 3.1 エントリー判定と最適化
1. **重み付けスコアリング**: RSI, VWAP, RVOL, ADX等の指標に重みを掛け合わせ、`threshold` 超過で判定。
2. **2段階進化型最適化**:
    *   **Phase 1 (広域探索)**: ランダムサンプリングにより、利益の出るパラメータ領域を特定する。
    *   **Phase 2 (局所進化)**: 上位解の周辺を重点的に探索（Mutation）し、小さな市場変化に動じない安定した解を選ぶ。
3. **評価関数 (Fitness Score)**:
    *   オーバーフィッティングを避けるため、単純な利益だけでなく「平均利益 ÷ 平均損失（RR比）」および「取引回数の安定性」を評価軸に据える。

### 3.2 エグジットおよびトレーリング
* **分割決済 (TP1)**: ATR 1.5倍到達時に一部利確し、リスクを低減。
* **トレンド追従 (TP2)**: シャンデリア出口により、トレンドが継続する限り利益を最大化する。

## 4. リスク管理設計
* **デイリー・ストップロス**: 1日の損益合計が -3.0% に達した時点で、新規エントリーを禁止。
* **固定損切り**: 各トレードに対し、最適化された損切り（SL）を厳守する。
