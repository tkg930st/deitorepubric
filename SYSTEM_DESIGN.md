# システム設計書：deitorepubric (Ver 15.15+)

## 1. システムアーキテクチャ
本システムは、解析（Analyzer）と監視（Monitor）の2フェーズで構成され、GitHub Actions上での自律運用を実現する。

### 1.1 コンポーネント構成
* **Analyzer**: `analyzer.py`, `backtest_engine.py`
    * **地合い連動型最適化**: 市場指数（N225）をバックテストに同期させ、悪地合いでのエントリーを避けるパラメータを自己学習する。
* **Monitor**: `monitor.py`, `position_manager.py`
    * **属性別精密フィルター**: 銘柄の特性（セクター・規模）に応じた個別指数（SOX/N225/TOPIX）ブレーキを執行直前にかける。
* **Verification**: `daily_retest.py`
    * 当日の実データを用いて Monitor の挙動を完全再現し、ロジック変更の有効性を数値で証明する。

## 2. データ設計
### 2.1 取引データの永続性 (堅牢化)
1. **Trade Journal (`trade_journal.csv`)**: 22カラムの構成を維持。
2. **Trade Results (`trade_results.csv`)**: 12カラム（logic_type含む）を厳守。
* **自動修復**: CSVの列数不一致や破損行がある場合、プログラムはエラーで停止せず警告を出してスキップする。

## 3. ログ出力設計
* **retest_log.txt**: `daily_retest.py` 実行時の全てのシミュレーション過程を記録。実運用のファイルを汚さない。

## 4. アルゴリズム・テクニカル指標の定義
1. **純粋テクニカルスコアリング**:
   - Analyzer と Monitor で判定基準（RSI/VWAP/RVOL/ADX）を完全同期（垂直同期）。
   - **後付けの加点ボーナスを全廃**し、バックテストの期待値と実機の挙動を 100% 一致させる。

## 5. リスク管理設計 (多重ディフェンス)
1. **地合い絶対フィルター**:
   - 市場全体が下落トレンド（Market Sentiment < -0.2）の際、LONGエントリーを物理的に遮断。
2. **属性別個別ブレーキ**:
   - ハイテク株は SOX指数、大型株は N225/TOPIX に連動して個別に遮断。
   - 小型株は地合い感度を緩和し、個別の優位性を優先。
3. **セクター連動確認**:
   - 同業種のライバル銘柄が上昇（LONG時）していることをリアルタイムで確認。
4. **SL下限ガード**:
   - どんな設定でも **0.7×ATR** より近い損切りは置かない。
5. **重複エントリー防止**:
   - 一度決済（損切り・利確問わず）された銘柄は、その日は同一銘柄への再エントリーを一切禁止し、過剰な取引を抑制。
6. **トレーリングストップによる利益保護**:
   - **TP1達成**: 残りポジションの損切りを建値（-0.2 ATR）へ即座に引き上げ、負けを排除。
   - **TP2トレーリング**: 高値（または安値）の更新に合わせて、一定間隔（1.0 ATR）で逆指値を動的に追従させ、利益を最大化。
7. **厳格なデータフェイルセーフ**:
   - 取引の根幹をなす指標データ（RSI, VWAP, MA15, ADX等）が `NaN` または `0.0` などの異常値となった場合、いかなる場合も評価を即座にスキップし、欠損データによる予期せぬ取引を完全に遮断する。

8. **時間軸（タイムディケイ）エグジット**:
   - **背景**: 過去55件のLONGトレード分析により、60分超えポジションの平均損益が -1.16%（vs 60分未満 -0.02%）であることが実データで確認された。
   - **Soft Time Limit (60分)**:
       - エントリーから60分経過後に、逆方向のシグナルスコア（SHORT側）が設定閾値を超えた場合、即座に現在価格で決済する。
       - `exit_reason: TIME_DECAY_COUNTER`
   - **Hard Time Limit (120分)**:
       - エントリーから120分経過した場合、シグナルや価格に関わらず無条件に強制決済する。
       - `exit_reason: TIME_MAX_LIMIT`
   - **実装ファイル**: `position_manager.py`, `monitor.py`, `backtest_engine.py`, `daily_retest.py`（完全同期）

## 6. 今後の拡張予定 (Future Roadmap)
1. **マルチ時間軸相関解析**:
   - 日足の長期トレンドと1分足の短期シグナルの相関性をバックテストに組み込む。
2. **SHORT戦略の更なる高度化**:
   - 下落局面での収益性を高めるため、空売り専用の指標閾値セットを独立最適化。

## 7. 品質保証 (必須工程)
1. **実データ検証 (`daily_retest.py`)**:
   - ロジックやパラメータの変更時は、必ず当日の実データでリテストを行い、改善効果を証明すること。
2. **解析ロジック同期テスト**:
   - 修正時は `test_monitor.py` で新旧データ混在時の堅牢性を検証すること。
